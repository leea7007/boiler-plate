//리듀서 끼리도 종류가 있고 그걸 다 모아서 정리해 쓰는 combineReducers


const { combineReducers } = require("redux");
// import user from './user_reducer';
const rootReducer = combineReducers({
  //user,
});

import(combineReducers);

export default rootReducer;
